---
title: 'Welcome / 欢迎'
description: 'Bilingual long-form sample post — replace with your own writing.'
pubDate: '2026-05-09'
lang: bilingual
---

import LangToggle from '../../components/LangToggle.astro';

<LangToggle />

<section id="en" class="lang-section">

## English

This is a placeholder long-form post. Replace it with your own writing. The site
is built with [Astro](https://astro.build) and supports MDX, so you can embed
React or Astro components inside posts.

### Features carried over from the reference site

- Long-form layout with table of contents
- Bilingual sections with anchor toggle (`#en` / `#zh`)
- Code blocks with syntax highlighting
- MDX for embedded interactive components
- Math via remark/rehype plugins (add later if needed)

```python
def hello(name: str) -> str:
    return f"hello, {name}"
```

</section>

<section id="zh" class="lang-section">

## 中文

这是一篇占位文章。把它换成你自己的内容就行。站点基于 [Astro](https://astro.build)，
支持 MDX，所以可以在正文里直接嵌入 React/Astro 组件。

### 从参考站照搬过来的功能

- 长文排版，自带目录
- 中英双语切换（锚点 `#en` / `#zh`）
- 代码块语法高亮
- MDX 嵌入交互组件
- 数学公式（后续按需接入 remark/rehype）

```python
def hello(name: str) -> str:
    return f"hello, {name}"
```

</section>
